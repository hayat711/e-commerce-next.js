/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://asset.msi.com/resize/image/global/product', 'https://asset.msi.com']
  }
}

module.exports = nextConfig
